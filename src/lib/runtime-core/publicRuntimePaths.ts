// Stable compatibility boundary: keep the current runtime route path unchanged
// until we intentionally migrate the public iframe contract.
export const PUBLIC_RUNTIME_API_BASE_PATH = '/api/kuailepu-runtime'
